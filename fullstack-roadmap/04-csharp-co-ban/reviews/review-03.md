# Spaced Review — sau bài 16

Đóng tài liệu khi trả lời; sau đó đối chiếu và làm một biến thể mới.

## Retrieval

1. Ôn Module 03: cleanup có tự phục hồi state khi lỗi không?
2. default Money bỏ qua kiểm tra constructor thế nào?
3. Queue và stack quyết định thứ tự xử lý/undo ra sao?
4. Namespace, assembly và project reference khác gì?
5. Vẽ candidate/list/item của capstone khi done gặp lỗi Save.

## Dự đoán output

1. Queue[A,B], xử lý A rồi undo bằng enqueue: lần tiếp theo lấy ai?
2. Lấy snapshot Pending trước done thành công, sau đó đọc snapshot cũ và snapshot mới: status nào?

## Debug

JSON có hai item cùng ID nhưng khác title. Nêu vì sao syntax hợp lệ chưa đủ, viết test reject không sửa file.

## Judgment liên module

File todo vài chục việc chuyển thành hai process ghi đồng thời: candidate+Move còn thiếu contract gì? Chọn bước nhỏ theo requirement, không tự chia microservices.

## Self-score

Retrieval 5 × 2, prediction 2 × 2, debug 3, judgment 3: tổng 20. Kết quả đúng thiếu trace state/cost nhận tối đa nửa điểm. Từ 16 điểm và không còn nhầm alias/commit thì đi tiếp; nếu chưa đạt, đọc phần liên quan rồi làm ví dụ khác. Nộp câu trả lời, trace và test expected/actual.

[Bản đồ module](../index.md).
